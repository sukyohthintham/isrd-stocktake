# ไฟล์ข้อมูลสต๊อกจริงจากระบบ — ห้ามขึ้น GitHub เพราะ Pages เป็น public
*.xlsx
*.xls
*.csv

# ตารางลิงก์รูปสินค้า — ไฟล์จริงเก็บใน sample-data/ แล้วอัปโหลดผ่านหน้า Master เอา
product-images*.json

# ไฟล์ล็อกที่ Excel สร้างตอนเปิดไฟล์ค้างไว้
~$*

# ขยะของ OS
Thumbs.db
desktop.ini
.DS_Store
sample-data/