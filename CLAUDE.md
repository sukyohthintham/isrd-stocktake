# ISRD Stocktake — เครื่องมือนับสต๊อก

## เครื่องมือนี้ทำอะไร
ให้ทีมคลังนับสต๊อกจริงด้วยมือถือ แล้วเทียบกับยอดในระบบ
เพื่อหาผลต่าง (variance) และสรุปผลการนับแต่ละรอบ

## กฎเหล็ก — ห้ามฝ่าฝืน
- ทั้งแอปอยู่ใน index.html ไฟล์เดียว (HTML + CSS + JS รวมกัน)
- ห้ามแตกเป็นหลายไฟล์ ห้ามเพิ่ม build step ห้ามใช้ npm หรือ bundler
- ไลบรารีภายนอกให้ inline ลงในไฟล์ ห้ามเรียกผ่าน CDN
  เหตุผล: GitHub Pages เคยโหลด CDN ไม่ติดจนแอปพังมาแล้ว
- ห้ามใช้ .set() เขียนทับ Firebase ทั้งก้อน ให้ใช้ .update() รายเรคอร์ดเสมอ
- ห้ามแก้โครงสร้างข้อมูล Firebase โดยไม่ถามก่อน

## ข้อกำหนดเฉพาะของงานนับสต๊อก
- ต้องรองรับหลายคนนับพร้อมกันโดยข้อมูลไม่ทับกัน
  ทุกการนับต้องบันทึกว่าใครนับ นับเมื่อไหร่ นับที่โซนไหน
- ห้ามลบยอดที่นับไปแล้ว ถ้าต้องแก้ให้บันทึกเป็นรายการใหม่ทับ
  แล้วเก็บของเดิมไว้เป็นประวัติ — ตรวจสอบย้อนหลังได้เสมอ

  **ข้อยกเว้นเดียว (v2.10.10) — เครื่องมือ "ล้างยอดของผู้ใช้ในรอบ"** ลบแถว scan จริง
  ใช้เก็บขยะจากคนยิงทดสอบ/ยิงผิดบัญชีเท่านั้น ไม่ใช่เครื่องมือแก้ยอดนับ

  ทำไมเขียนทับแบบปกติไม่ได้: leaderboard "ผู้ยิงในรอบนี้" นับยอดดิบต่อคนจาก `rec.user`
  การเขียนแถวลบทับจะลงชื่อ "คนที่กดลบ" ไม่ใช่คนที่ยิงเดิม — คนทดสอบยังโชว์ยอดเต็ม
  ส่วนแอดมินโดนหักยอดตัวเอง แก้ด้วยการเขียนทับไม่ได้โดยธรรมชาติ

  เงื่อนไขที่ต้องคงไว้ ห้ามผ่อน:
  admin เท่านั้น · เฉพาะรอบ `counting` · เขียน `rounds/{id}/purgeLog`
  (สำเนาแถวเต็มทุกฟิลด์ ไว้กู้ด้วยมือ) ให้สำเร็จก่อนจึงลบได้ · ปุ่มอยู่ในการ์ดที่กางแล้วเท่านั้น
  Rules ให้ admin "ลบ" ได้อย่างเดียว — แก้ทับแถว scan ยังห้ามทุกคนทุกสถานะ
  guard `tests/test-purge-user.js` และ `tests/test-rules.js` — แก้เมื่อไหร่ต้องอัปเดตทั้งคู่
- แยกข้อมูลตาม "รอบนับ" ชัดเจน รอบเก่าต้องไม่ปนกับรอบใหม่
- ต้องมีหน้าสรุปผลต่างระหว่างยอดนับจริงกับยอดระบบ
- ต้อง export ผลออกเป็น Excel ได้

## กฎแถวผี 0/0 (v2.8.0) — กฎบ้าน ห้ามแก้โดยไม่ถาม

**สินค้าที่ระบบ = 0 และนับได้ = 0 ตัดทิ้งเสมอ** ทั้งในเอกสาร ยอดรวม และ %Success
ต่อให้มีในทะเบียน Master ก็ตาม

เหตุผล: 0/0 แปลว่าไม่มีของจริงและไม่ได้นับเจออะไรเลย ไม่ใช่แถวที่ต้องรายงาน
ถ้าปล่อยไว้มันจะถูกนับเป็น "SKU ที่ตรง" เพราะ `act === sys` (0 = 0)
แล้ว **%Success (SKU) จะเฟ้อ**ตามจำนวนสินค้าที่สาขานั้นไม่ได้ขาย ซึ่งมีเป็นพันรายการโดยธรรมชาติ
(เคสจริง: มี P1 ตัวเดียวได้ 0% · พอมีแถว 0/0 มาด้วยกลายเป็น 50%)

### กฎนี้มีที่เดียว — `isGhostRow(sys, act)`
เรียกจาก 3 จุด ห้ามเขียนเงื่อนไข 0/0 ซ้ำที่อื่น:
`summaryData()` · `summaryDataFrom()` (โหมดรวมทั้งรอบ) · `loadCycleStats()` (หัวรอบนับ)

ก่อน v2.8.0 กฎนี้เขียนไว้ 3 ที่ด้วยเงื่อนไขไม่ตรงกัน — เอกสารตัด 0/0 ทิ้ง แต่ยอดรวมไม่ตัด
ตารางกับยอดรวมในเอกสารใบเดียวกันจึงไม่ตรงกัน คนอ่านกระทบยอดไม่ได้

### ตัดเฉพาะ 0/0 สุทธิเท่านั้น
แถวที่มียอดนับจริง (`act ≠ 0`) หรือมียอดระบบ (`sys ≠ 0`) **ต้องไม่ถูกตัด** — กฎบ้าน "ห้ามทำยอดหาย"
เคสอื่นเป็นคนละเรื่อง มีเงื่อนไขของตัวเองอยู่ที่ผู้เรียก ห้ามเอามารวมกับกฎนี้:
ยอดติดลบ · บาร์โค้ดที่แอดมินสั่งลบทิ้ง (`discarded`) · ยอดที่ถูกย้ายไปผูกกับสินค้าอื่น

guard ด้วย `tests/test-doc-groups.js` ข้อ [4] (0/0 ต้องหายทั้งตารางและยอดรวม)
และ [4b] (ของสาขาอื่นที่ยังมียอดนับจริงต้องไม่ถูกตัด) — แก้กฎนี้เมื่อไหร่ต้องอัปเดตทั้งคู่

## ตารางสิทธิ์ 4 role (v2.9.0) — กฎบ้าน ห้ามแก้โดยไม่ถาม

| ความสามารถ | admin | counter | scanner | viewer |
|---|:---:|:---:|:---:|:---:|
| ยิงบาร์โค้ด | ✓ | ✓ | ✓ | ✗ |
| สร้าง / ปิด Job | ✓ | ✓ | ✗ | ✗ |
| ออกเอกสาร (ดู + พิมพ์) | ✓ | ✓ | ✗ | ✓ |
| ค้นหาสินค้า + Location | ✓ | ✓ ดูอย่างเดียว | ✗ | ✗ |
| แก้ไข Master / Location | ✓ | ✗ | ✗ | ✗ |
| พิมพ์ Remark (ตอนยิง) | ✓ | ✓ | ✓ | ✗ |
| เห็นรายการ Job | ✓ | ✓ | ✓ | ✓ |
| เข้าหน้า start / summary | ✓ | ✓ | ✗ | ✗ |

- **viewer เห็นได้แค่ "รายการ Job + เอกสาร"** เท่านั้น ดูเอกสารและสั่งพิมพ์ได้ แต่แก้หมายเหตุหรือยอดไม่ได้
- **scanner พิมพ์ Remark ได้เฉพาะ "ตอนยิง"** (หน้า scan รอบที่ยัง counting) ไม่ใช่ในหน้าเอกสาร
- **Master / Location เป็นข้อมูลกลางของทุกสาขา แก้ได้เฉพาะ admin** — counter เข้าดูและค้นหาได้ แต่ไม่มีช่องกรอกให้

### ต้องล็อกสองชั้นเสมอ (defense-in-depth)
1. **ชั้นจอ** — ซ่อนหรือ disable ปุ่ม/ช่อง ไม่ใช่ปล่อยให้กดแล้วค่อยเด้ง
2. **ชั้นฟังก์ชัน** — เช็คสิทธิ์ซ้ำในฟังก์ชันที่เขียนข้อมูล กันคนแก้ DOM หรือเรียกจาก Console
   ใช้ `roleBlockMessage()` บอกเหตุผลเป็นภาษาคนเสมอ

### Rules ต้องตรงกับ UI เสมอ
สิทธิ์จริงบังคับที่ `stocktake-rules-v2.1.1.json` (Firebase Console → Realtime Database → Rules)
แก้สิทธิ์ใน index.html แล้วต้องเช็ค Rules ทุกครั้ง ไม่ตรงกันเจ็บสองแบบ:
- Rules หลวมกว่า UI = มีรูให้เขียนข้อมูลได้ทั้งที่จอไม่ให้ทำ
- Rules แน่นกว่า UI = จอเปิดให้กด แต่กดแล้วเด้ง "ฐานข้อมูลปฏิเสธการเขียน"

`tests/test-rules.js` ประเมินนิพจน์ `.write` ตัวจริงทุก path × role × สถานะ Job
`tests/test-role-matrix.js` คุมสิทธิ์ฝั่ง UI ทั้งสองชั้น — แก้สิทธิ์เมื่อไหร่ต้องอัปเดตทั้งคู่

**แก้ไฟล์ Rules แล้วยังไม่มีผล** ต้องเอาไปวางที่ Firebase Console เองทั้งไฟล์แล้วกด Publish
และต้องมีบล็อก `wms2026` ติดไปด้วยเสมอ ไม่งั้น WMS Dashboard ที่ใช้ฐานเดียวกันจะเขียนไม่ได้ทันที

## Stack
- Firebase Realtime Database — region asia-southeast1
- Deploy: GitHub Pages — user sukyohthintham
- ไม่มี backend server

## แนวทาง UI
- ข้อความที่ผู้ใช้เห็นเป็นภาษาไทยทั้งหมด
- ชื่อตัวแปรและฟังก์ชันเป็นภาษาอังกฤษ
- คอมเมนต์เป็นภาษาไทย — ทั้งไฟล์เขียนไทยอยู่แล้ว เขียนตามให้เหมือนกันทั้งไฟล์
  คอมเมนต์ที่ดีต้องบอก "ทำไมถึงเขียนแบบนี้" ไม่ใช่แค่บอกว่าโค้ดทำอะไร
- ผู้ใช้คือทีมคลังสินค้า ไม่ใช่โปรแกรมเมอร์
  ข้อความ error ต้องบอกเป็นภาษาคนว่าให้ทำอะไรต่อ
- ออกแบบสำหรับมือถือเป็นหลัก ปุ่มใหญ่พอสำหรับคนใส่ถุงมือ
- ช่องกรอกจำนวนต้องเรียกแป้นตัวเลขขึ้นมาอัตโนมัติ

## เวอร์ชัน
อัปเดตเลขเวอร์ชัน 3 จุดพร้อมกันทุกครั้ง — <meta name="version">, <title>, และ var APP_VERSION

## การอนุมัติก่อน push
- ห้าม git commit / git push เองเด็ดขาด — โชว์ git diff แล้วรอผู้ใช้อนุมัติก่อนเสมอ
- ก่อนบอกเสร็จ ต้องรัน regression เต็มชุด (node ทุกไฟล์ test-*.js) ให้ตก 0 ก่อน

## ก่อนบอกว่าเสร็จ
- ทดสอบด้วย Puppeteer headless Chrome
- สรุปสั้น ๆ ว่าแก้อะไร กระทบส่วนไหนบ้าง